#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    while(cin >> n) {
        if(n != 1999)
            cout << "Wrong" << endl;
        else {
            cout << "Correct" << endl;
            break;
        }
    }

    return 0;
}
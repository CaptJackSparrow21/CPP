#include<bits/stdc++.h>
using namespace std;
#define int long long

signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int m; cin >> m;
    vector<int> a(m), b(m);
    for(int &i : a) cin >> i;
    for(int &i : b) cin >> i;
    
    

    return 0;
}